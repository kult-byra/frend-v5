If stuff breaks, try:

`pnpm remove-all && pnpm install -r`


# Pre commit
Running lefthook for pre commits. If GH desktop fails, try to restart.

If you change `lefthook.yml`you need to run `pnpm lefthook install` to update the git hooks.
{
  "name": "next-sanity-turbo",
  "version": "0.0.3",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "biome check .",
    "check": "biome check . --write",
    "format": "biome format . --write",
    "studio": "pnpm --filter studio",
    "frontend": "pnpm --filter frontend",
    "typegen": "SKIP_VALIDATION=true pnpm --filter studio exec sanity schema extract --workspace no --path ../frontend/schema.json --enforce-required-fields && pnpm --filter frontend exec sanity typegen generate && node scripts/prepare-types.js",
    "typecheck": "pnpm frontend typecheck && pnpm studio typecheck",
    "remove-all": "rm -rf node_modules && rm -rf pnpm-lock.yaml && rm -rf .turbo && rm -rf apps/*/node_modules && rm -rf apps/*/pnpm-lock.yaml && rm -rf packages/*/node_modules && rm -rf packages/*/pnpm-lock.yaml"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.11",
    "@sanity/cli": "^5.5.0",
    "@workspace/typescript-config": "workspace:*",
    "lefthook": "^2.0.14",
    "turbo": "^2.7.5",
    "typescript": "5.9.3"
  },
  "packageManager": "pnpm@10.28.1",
  "engines": {
    "node": ">=24"
  }
}
